drive toward a gui interactive test harness for ttt
- predicate combine (reversed arguments)
- generalize the rehman tree (decouple from combine?)


- more cleanup, as always.


extract test code into it's own dependency and move its images somewhere
change test code to use XHR and script comparison rather than http-equiv refresh.
[because i am annoyed with the blinking and having to press a button to stop the refresh. idea: stop refresh when tab is in foreground. See https://developer.mozilla.org/en-US/docs/Web/API/Page_Visibility_API?redirectlocale=en-US&redirectslug=DOM%2FUsing_the_Page_Visibility_API]
